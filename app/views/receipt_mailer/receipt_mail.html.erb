<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Hi <%= @order.email %></h1>

            <% @order.line_items.each do |line_item| %>
          <%= render 'text', product_id: line_item.product_id, quantity: line_item.quantity%>
        <% end %>
  </body>
</html>